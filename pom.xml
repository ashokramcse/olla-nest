<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<!-- ===================================================================
	     Olla Nest — Parent POM
	     Manages three modules:
	       olla-nest-common  — shared services, models, connectors, config
	       olla-nest-admin   — admin control panel  (port 8080)
	       olla-nest-user    — employee workspace   (port 8081)
	     Runtime : Oracle Java 26 JVM  |  Spring Boot 3.5.14
	     =================================================================== -->

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.14</version>
		<relativePath />
	</parent>

	<groupId>com.ollanest</groupId>
	<artifactId>olla-nest-parent</artifactId>
	<version>2026.1.5</version>
	<packaging>pom</packaging>
	<name>olla-nest-parent</name>
	<description>Olla Nest – Parent POM</description>

	<modules>
		<module>olla-nest-common</module>
		<module>olla-nest-admin</module>
		<module>olla-nest-user</module>
	</modules>

	<properties>
		<java.version>26</java.version>
		<maven.compiler.release>21</maven.compiler.release>
		<spring-ai.version>1.0.0</spring-ai.version>
		<pdfbox.version>3.0.7</pdfbox.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.ai</groupId>
				<artifactId>spring-ai-bom</artifactId>
				<version>${spring-ai.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<!-- Internal modules -->
			<dependency>
				<groupId>com.ollanest</groupId>
				<artifactId>olla-nest-common</artifactId>
				<version>${project.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

</project>
