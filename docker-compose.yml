services:
  app:
    build: .
    container_name: olla-nest-app
    restart: unless-stopped
    environment:
      PORT: 3000
      OLLA_NEST_DOCKER_RUNTIME: "true"
      STORAGE_MODE: local
      DEFAULT_ADMIN_EMAIL: ${DEFAULT_ADMIN_EMAIL:-admin@ollanest.local}
      DEFAULT_ADMIN_PASSWORD: ${DEFAULT_ADMIN_PASSWORD:-CHANGE_ME_ON_FIRST_BOOT}
      DEFAULT_USER_PASSWORD: ${DEFAULT_USER_PASSWORD:-CHANGE_ME_ON_FIRST_BOOT}
      OLLAMA_URL: ${OLLAMA_URL:-http://host.docker.internal:11434}
      SECRET_KEY: ${SECRET_KEY:-}
      SESSION_SECRET: ${SESSION_SECRET:-change-this-in-production}
      SQLITE_PATH: /app/data/olla-nest.sqlite
      DOCUMENT_DB_PATH: /app/data/documents.json
      WORKSPACE_ROOT: /app/data/workspace
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ${HOME}:/mac-home
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '0.5'
          memory: 512M
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/bootstrap', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
