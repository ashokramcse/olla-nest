services:
  app:
    build: .
    container_name: olla-nest-app
    restart: unless-stopped
    environment:
      PORT: 3000
      OLLA_NEST_DOCKER_RUNTIME: "true"
      STORAGE_MODE: local
      DEFAULT_ADMIN_EMAIL: ${DEFAULT_ADMIN_EMAIL:-admin@ollanest.local}
      DEFAULT_ADMIN_PASSWORD: ${DEFAULT_ADMIN_PASSWORD:-ChangeMe!CreateARealPassword123}
      DEFAULT_USER_PASSWORD: ${DEFAULT_USER_PASSWORD:-UserDemo!12345}
      OLLAMA_URL: ${OLLAMA_URL:-http://host.docker.internal:11434}
      SQLITE_PATH: /app/data/olla-nest.sqlite
      DOCUMENT_DB_PATH: /app/data/documents.json
      WORKSPACE_ROOT: /app/data/workspace
    ports:
      - "3000:3000"
    volumes:
      - app-data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3000/api/bootstrap').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 5

volumes:
  app-data:
